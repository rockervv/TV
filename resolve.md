# TV 應用效能與穩定性修復總結 (2026-08-07)

## 1. 核心問題：UI 執行緒鎖死 (Deadlock/Blocking)
*   **現象**：進入播放詳情頁、返回首頁或背景搜尋時，介面完全無響應，返回鍵失效。
*   **根本原因**：
    *   **Map 桶鎖競爭**：原先使用 `ConcurrentHashMap.computeIfAbsent` 進行 Spider 初始化。在 Android 實作中，此方法會鎖定雜湊桶 (Bucket)。當背景執行緒正在執行耗時的 `spider.init()`（如網路請求 5-7 秒）時，UI 執行緒若嘗試獲取同一個桶位中的任何 Spider，會被強行卡死。
    *   **日誌系統過載 (Log Storm)**：當站台被封鎖返回巨量 HTML 時，拋出的 `JSONException` 包含數萬字元。系統在處理與列印這些超長字串時佔滿 CPU，導致主執行緒無法處理按鍵事件。
*   **解法**：
    *   **實施分段鎖 (Striped Locking)**：改用 `synchronized(key.intern())` 配合雙重檢查鎖 (Double-Checked Locking)。確保初始化站台 A 絕不會卡住正在獲取站台 B 的 UI 執行緒。
    *   **日誌截斷**：在 `SpiderDebug` 中實施 1000 字元強制截斷，防止垃圾資料炸毀 `logd` 服務。

## 2. 搜尋結果遺漏與「無資料」
*   **現象**：搜尋時只出現極少數站台，或明明有站台卻顯示無資料。
*   **根本原因**：
    *   **判定邏輯回歸**：`isSpider` 判定過於嚴苛（曾誤限制為僅 `type == 3`），導致所有 `csp_` 開頭的 JAR 站點被歸類為普通 API 走錯路徑。
    *   **執行緒飢餓**：執行緒池大小 (8) 不足以應付 61 個站台的併發請求，導致排隊中的站台集體超時 (Timeout)。
    *   **分數過濾陷阱**：之前的機制會永久跳過分數為負的站台，導致被封鎖過一次的站台永久消失。
*   **解法**：
    *   **恢復判定**：重新開放 `csp_`, `.js`, `.py`, `js_` 以及「具有 jar 檔案」的所有站台走 Spider 流程。
    *   **擴張執行緒池**：將 `largeExecutor` 從 8 提升至 **30**，確保全域搜尋能瞬間啟動所有任務。
    *   **取消硬性過濾**：搜尋時不再排除負分站台，改為「好站優先排序」，保證結果完整性。

## 3. 導航邏輯修復
*   **現象**：在詳情畫面（非全螢幕）下，按返回鍵無法回到首頁。
*   **根本原因**：`VideoActivity` 的 `handleBack()` 始終返回 `true`，導致 Activity 攔截了所有返回事件而不執行 `finish()`。
*   **解法**：修正邏輯，僅在全螢幕模式下攔截返回鍵用於「退出全螢幕」；在一般模式下返回 `false` 允許 Activity 正常關閉。

## 4. 系統健壯性優化
*   **HTML 快速攔截**：在 `SiteApi` 進入昂貴的 JSON 解析前，增加 `isHtml` 預檢（檢測 `<!DOCTYPE`, `Access Denied` 等）。若為 HTML 垃圾資料則在 1ms 內立即丟棄。
*   **防止 NPE 崩潰**：在 `VideoActivity` 的延遲任務（如 `hideProgress`）中加入 `player()` 非空檢查，防止因 Service 斷開連結引發的閃退。
*   **減輕 UI 壓力**：在 `SiteViewModel` 中加入批處理判定，只有當站台真的有搜尋結果時才通知 UI 刷新，避免 50+ 個站台回傳空資料引發的重複渲染。

## 5. 激勵機制與 UI 反饋 (2026-08-07)
*   **站台分數可視化**：在首頁站台切換清單中，於站台名稱後方增加 `[分數]` 顯示。
    *   **目的**：落實「良幣驅逐劣幣」，讓使用者直觀看到站台的穩定性評分，並驗證分數機制是否正常運作（成功搜尋加分，失敗/超時扣分）。
*   **載入提示 (Notify) 優化**：
    *   **現象**：手動切換站台時「正在載入」提示偶爾長駐不消失。
    *   **原因**：`Notify.showTop` 採用異步 `Handler.post` 彈出。當網路請求極快返回並觸發 `dismissTop` 時，對話框可能尚未正式創建/顯示，導致 `dismiss` 動作失效，隨後延遲的 `post` 任務才將對話框顯示出來並永久駐留。
    *   **解法**：在 `Notify` 中引入 `mShowRunnable` 實體引用，確保 `dismissTop` 調用時能同步取消尚未執行的顯示任務，徹底解決競爭風險。
*   **首頁推薦更新機制優化**：
    *   **現象**：切換站台時，下方的推薦影片可能顯示錯誤或未及時清空舊站台數據。
    *   **解法**：重構 `HomeFragment.addVideo` 邏輯，確保在添加新資料前強制清空舊有的推薦列表。
*   **強制重新整理功能**：
    *   **實作**：為首頁標題增加長按監聽，觸發 `onRefreshHome`。此操作會主動清除記憶體快取並發起帶有 `refresh=true` 標記的請求，確保繞過快取直接獲取站台最新資料。
